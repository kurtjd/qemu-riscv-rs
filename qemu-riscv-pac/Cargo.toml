[package]
name = "qemu-riscv-pac"
version = "0.1.0"
edition = "2024"
description = "Peripheral Access Crate (PAC) for the QEMU RISC-V virt platform"
authors = ["Kurtis Dinelle <kurtis.dinelle@gmail.com>"]
license = "MIT"
keywords = ["embedded", "risc-v", "qemu", "pac", "no-std"]
repository = "https://github.com/kurtjd/qemu-riscv-rs"

[dependencies]
critical-section = { version = "1.2.0", optional = true }
riscv = "0.16.0"
riscv-peripheral = "0.5.0"
riscv-rt = { version = "0.17.0", features = [
    "device",
    "memory",
], optional = true }
vcell = "0.1.3"

[features]
rt = ["riscv-rt"]
v-trap = ["rt", "riscv-rt/v-trap"]
